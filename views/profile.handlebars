  <header>
    <h1>Welcome to the Campfire</h1>
  </header>

<div class="profile">
     <div>
        <h2>name: <a href="/user/{{id}}">{{username}}</a> </h3>
        <section class="card">   
            <h3>{{id}}</h3><br>
            <h3>{{email}}</h3><br>
            <p>{{bio}}</p><br>
    </section>
    </div>
</section>